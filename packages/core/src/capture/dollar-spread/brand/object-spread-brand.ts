/**
 * Type-only key that marks an object pattern as written with `{ ...$ }`.
 *
 * Declared and never defined: the runtime never reads it.
 */
export declare const OBJECT_SPREAD_BRAND: unique symbol

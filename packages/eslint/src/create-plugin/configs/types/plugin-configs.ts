import type { RecommendedConfig } from '@ts-unify/eslint/create-plugin/configs/recommended'

/**
 * The configs a plugin ships: `recommended` alone.
 */
export type PluginConfigs = { recommended: RecommendedConfig }

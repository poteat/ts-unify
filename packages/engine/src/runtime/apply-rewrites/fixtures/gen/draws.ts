/**
 * What the generators draw from: the identifier names, the odds of a
 * literal over an identifier, and the range of a literal's value.
 */
export const DRAWS = {
  names: ['a', 'b', 'c', 'd', 'e'],
  literalOdds: 0.5,
  literalRange: 100,
} as const

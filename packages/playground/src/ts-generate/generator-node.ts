/**
 * A node as the generator reads it: a `type` tag and whatever fields that
 * tag implies.
 *
 * The generator reads the fields without a schema, as astring's own does.
 */
// eslint-disable-next-line atp/no-any
export type GeneratorNode = any

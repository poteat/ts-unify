/**
 * A sample program the playground offers: its menu key and label, and the
 * source it loads into the editor.
 */
export type Scenario = {
  readonly key: string
  readonly label: string
  readonly code: string
}
